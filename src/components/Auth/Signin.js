import React from 'react';
import { Link } from 'react-router-dom';

const Signin = () => {
  return (
    <div className="">
      {Signin.map((item, idx) => (
        <Link />
      ))}
      <Signin />
    </div>
  );
};

export default Signin;
